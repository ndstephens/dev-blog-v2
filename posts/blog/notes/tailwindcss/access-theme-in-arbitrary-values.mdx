---
title: Access the Theme in Arbitrary Values
description: It's possible to use the `theme` function within arbitrary values to reference the design tokens in your `tailwind.config.js` file.
created: 2023-02-11
updated: 2023-02-11
category: notes
topics:
  - Tailwind
---

It's possible to use the `theme` function within **arbitrary values** to reference the design tokens in your `tailwind.config.js` file.

```html /theme/
<div class="grid grid-cols-[fit-content(theme(spacing.32))]">
  <!-- ... -->
</div>
```

```jsx /theme/
<TopicsFilter
  currentTopic={topicParam}
  topics={postTopics}
  category={category}
  className="md:sticky md:top-[calc(theme(spacing.headerHeight)+3rem)] md:[grid-area:filter]"
/>
```

<br />

<TailwindDocs />

---

<LastUpdated updated={updated} />
